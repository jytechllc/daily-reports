### Marin Varivoda
1. Daily report — Phase 2 support foundation (schema + domain)
2. Schema — `customers`, `customer_contacts`, `ticket_messages`, `knowledge_snippets` in `jytech_internal`; migration `0006_wonderful_morlocks.sql` applied; `tickets.customer_id` + SLA indexes wired to orgs/projects/users.
3. Tickets — `lib/support/db.ts` covers create/update/list/soft-delete, filters (status, project, assignee, priority, channel, SLA risk), auto customer link via `findOrCreateCustomerByEmail`, and message thread with internal vs customer-visible replies.
4. SLA — `lib/support/sla.ts` maps priority → due time (urgent 4h → low 7d), computes `ok` / `at_risk` / `breached`, recalculates on priority change, sets `resolvedAt` on resolve/close.
5. Dashboard-ready queries — `getSupportDashboardStats()` (open/pending, SLA risk/breach, avg age, volume by project), `listTicketsAtSlaRisk`, customer + snippet CRUD — ready for Day 2 APIs and inbox UI.
