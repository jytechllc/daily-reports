### Marin Varivoda
1. Daily report — Phase 2 support APIs
2. Tickets API — `GET/POST /api/support/tickets` with filters (status, project, assignee, priority, SLA risk, sort).
3. Ticket detail — `GET/PATCH /api/support/tickets/[id]` for assign, status, priority, escalate.
4. Messages API — `GET/POST /api/support/tickets/[id]/messages` for thread + internal/customer replies.
5. Audit + snippets — audit on create/assign/status/escalate; `GET/POST /api/support/snippets` for canned replies.

### Mengchun Jiang
1. Daily report:
2. Finished the desktop i18n cleanup. All shell dialogs localized via a unit-tested en/zh/zh-TW/ko strings table, native menus/tray now follow in-app language switches instantly, and zh-HK/zh-MO systems get Traditional Chinese.
3. Localized the demo dashboard end to end. The API now returns structured facts rendered through client dictionaries (greeting by hour, chart tooltip, relative timestamps included).
