### Mengchun Jiang
1. Daily report:
2. Reconnect now backs off 5s to 30s instead of a fixed 5s interval that aborted its own slow loads.
3. A renderer that keeps crashing now stops auto-reloading after 3 crashes in 60s and shows a Retry page.
4. Window position is dropped when the display it was saved on is gone, so the app can't reopen off-screen.
5. Close-to-tray now requires a tray to exist, so a failed tray icon no longer leaves an unclosable resident app.
6. Hang prompts are held while the window is hidden, instead of blocking the main process behind an invisible modal.
7. Test files no longer ship inside the packaged asar.

Plan:
1. add CSP injection from the main process plus failure-state feedback and retry entry points.

### Marin Varivoda
1. Daily report -  Auth0 app inventory per project(Phase 1)
2. Adapter—`lib/auth0-mgmt.ts` lists apps + APIs (paginated) with owner from `client_metadata`.
3. Schema—`auth0` metric source +`auth0_app_ids`;
4. Sync— project-metrics cron writes per-project`auth0_apps`(+ org`auth0_tenant`).
5. Auth tab— snapshot-first inventory; linked IDs, matched apps/APIs,no ownerflags.
