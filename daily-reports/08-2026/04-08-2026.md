### Mengchun Jiang
1. Daily report:
2. Opened PR #38: desktop packaging hardening plus five demo-API fixes.
3. Packaged binary no longer doubles as a Node interpreter or loads code outside app.asar.
4. Permission allowlist now covers the check path, which previously fell back to Electron defaults.
5. Demo chat credit cap is atomic — 20 concurrent turns now grant 1, not 20.
6. Demo login is rate-limited per IP and no longer blocks the event loop on scrypt.
7. Knowledge ingest no longer publishes half-written documents into retrieval.
8. Demo sessions can no longer be minted offline from the in-repo default secret.

Plan:
1. Cut installer size and add a size assertion.

### Marin Varivoda
1. Daily report - Project registry in DB(Phase 1 )
2. Schema— Extended`projects`with category, zh description, tags, github repos, vercel name; migration`0004_neat_night_thrasher.sql`.
3. Read/write—`list`/`get`/`upsert`/`update`/`softDelete`+`seedProjects`; shared`ensureDefaultOrg`in`lib/org.ts`.
4. Runtime— Projects page, People allocation, and assistant tools load from DB (`loadProjects`); static array is seed-only.
5. Ops—`npm run db:seed-projects`(optional`--force`); empty table auto-seeds on first load.
