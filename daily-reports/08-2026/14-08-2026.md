### Marin Varivoda
1. Daily report — Phase 2 support inbound
2. Public form — `/support/form` (no auth): product, name, email, subject, body; success reference id.
3. Public API — `POST /api/support/public/tickets` with honeypot + IP rate limit (5/15 min); `form` channel + customer auto-link by email.
4. Manual create — `/support` form for support/admin: channel, priority, project, assignee, customer fields → `POST /api/support/tickets`.
5. Auth bypass — proxy skips Auth0 for `/support/form` and `/api/support/public/*`; exec sees intake info, create restricted to support/admin.
