### Mengchun Jiang
1. Daily report:
2. The shell now injects its own CSP on every app-origin document from the main process via onHeadersReceived, appended as a second header so it can only tighten the site's policy.
3. Added electron/csp.js as a pure unit-tested module (policy build, document filter, header append) with 9 new tests, 348 total green plus tsc and next build.
4. Gave every failed demo request a visible failure state: campaign view got loading/failed/empty states with Retry, chat keeps the failed message and redelivers once on Retry, wizard and knowledge uploads no longer die as unhandled rejections.

Plan:
1. Start i18n cleanup.

### Marin Varivoda
1. Daily report -  Per-project health (uptime / AWS)
2. Matching—`infra-health.ts`maps JYTECH_APPS + EC2/EB to projects (`up`/`down`/`degraded`/`unknown`).
3. Snapshots— cron writes`uptime`/`infra_health`; Infra tab prefers snapshots, live fallback.
4. List badges—`/projects`shows health dots + Healthy/Unhealthy stats.
5. Infra tab— health status + clearer empty states; Quantum Med URL seeded for uptime match.
