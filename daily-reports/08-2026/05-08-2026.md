### Mengchun Jiang
1. Daily report:
2. Opened PR #39: the Windows desktop installer was shipping the entire web dependency tree.
3. Desktop CI now installs with npm ci and gives the electron tree its own working-directory step.
4. Builds abort up front when the app dependency tree is incomplete, instead of quietly producing a 313 MiB installer.
5. Added a size ceiling that fails the build on any installer over 150 MiB.

Plan:
1. Add IME-safe send and honest save state.

### Marin Varivoda
1. Daily report - Project detail page(Phase 1 )
2. Route - `/projects/[slug]` with chip tabs Code / Work / Auth / Infra (`?tab=`).
3. Live data -  Code (commits, boards, Vercel), Work (GitHub/Jira tasks + staffing), Auth (Auth0 Mgmt when configured), Infra (uptime + AWS/Vercel).
4. Matching - `lib/projects/match.ts`+ tab-scoped `loadProjectDetail` so each tab only fetches what it needs.
5. List wiring -  project cards link into the detail page.
