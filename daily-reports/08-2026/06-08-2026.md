### Mengchun Jiang
1. Daily report:
2. Opened PR #40 — desktop batch 5, four frontend and shell P1 fixes.
3. Chat composer no longer sends half-typed words during CJK IME composition.
4. Settings now surface save failures instead of always showing "Saved ✓".
5. Launch-on-startup and minimize-to-tray switches now drive the shell over IPC.
6. Window zoom no longer reverts to the startup value after a reload.
7. Confirmed Chromium keeps zoom per origin per session, correcting a backlog assumption.

Plan:
1. Add reconnect backoff, crash-loop breaker, window placement checks.

### Marin Varivoda
1. Daily report - GitHub + Jira → metrics_snapshots(Phase 1)
2. Sync job - Org-wide GitHub/Jira fetch, fan-out per project into `metrics_snapshots`(`code_30d`+`open_tasks`).
3. Cron - `/api/cron/project-metrics` every 2h (`vercel.json`), CRON_SECRET-gated like the other jobs.
4. UI- Code/Work tabs prefer snapshots (≤36h), fall back to live; show Snapshot vs Live.
5. Config - Jira accepts`JIRA_SITE_URL` or `JIRA_BASE_URL`; GitHub config accepts org token.
