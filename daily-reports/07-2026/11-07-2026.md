### Mengchun Jiang
1. Daily Report:
2. Shipped aichat Fast Mode: tool-free, token-streamed responses with per-provider timeouts supported by Cerebras (#90).
3. Fixed auto-mode fallback to a working Cerebras model so free-tier chat stops erroring (#91).
4. Gated the ECS/Fargate deploy workflow to no-op until AWS infra vars are set, keeping CI green ahead of P3 infra + creds (#92).

Plan:
1. Flip on ECS deploy once P3 networking, real credentials, and region are locked.
