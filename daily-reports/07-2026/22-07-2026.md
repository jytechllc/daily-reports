### Marin Varivoda
1. Daily report - Shared UI kit (Phase 0)
2. Created branch `feat/shared-ui-kit` to extract dashboard primitives into a reusable module kit.
3. Split `Card` / `Stat` / `Dot` / `ErrorNote` / `SectionSkeleton` and `money` / `compact` into `app/_components/ui/` with a single barrel export (removed the old monolithic `ui.tsx`).
4. Added shared primitives modules will need next: `PageHeader`, `Button`, `Badge`, and `EmptyState` - same tokens/look, no new dependencies.
5. Migrated `ModulePlaceholder`, Admin, and Forbidden to the kit
