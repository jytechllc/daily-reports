### Mengchun Jiang
1. Daily Report:
2. Added a graphics-acceleration fallback to the AutoClaw desktop shell for machines where GPU compositing renders a black or invisible window.
3. Implemented three escape hatches: a persisted "Disable Graphics Acceleration" menu toggle, an AUTOCLAW_DISABLE_GPU env var, and automatic fallback after 3 GPU process crashes with a silent relaunch when it happens before the first window.

Plan:
1. Cut the next desktop release so the fallback ships via auto-update.
