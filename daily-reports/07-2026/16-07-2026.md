### Mengchun Jiang
1. Daily Report:
2. Wired all six AutoClaw desktop-preview views to a seeded local Postgres via a new auth-free `/api/demo/*` namespace, replacing the static mock data (Leads, Dashboard, Quickstart, Campaign, Inbox, Settings).
3. Moved Quickstart under Dashboard reusing the setup wizard, and made its launch persist the config/template and insert "found" leads so the loop is visible across Dashboard/Leads/Campaign.
4. Guarded the demo namespace to be inert in production (404 unless DEMO_MODE=1) and fall the views back to mock when the demo API is unavailable, keeping the deployed preview unchanged.
5. Opened PR #12 on jytechllc/autoclaw-app and tracked the still-stubbed pieces in docs/desktop-demo-remaining.md.

Plan:
1. finish the remaining stubs next session — Inbox AI draft/translate, real outbound send, and real lead discovery.

### Marin Varivoda
1. Daily report - App shell (Phase 0):
2. Created branch `feat/app-shell`for the Phase 0 shell work.
3. Added shared app layout with sidebar nav and mobile top nav for all modules.
4. Wired 7 module routes: Dashboard, Projects, Support, People, Budget, Market, AI Agent.
5. Moved the existing AWS monitor dashboard into`/`under the new shell (sections unchanged).
6. Added placeholder pages for the 6 new modules; kept Auth0 sign-in/out + open-mode warning and updated README layout notes.
