### Marin Varivoda
1. Daily report - Audit log write path (Phase 0)
2. Created branch `feat/audit-log` to add a reusable audit write path on the existing `audit_logs` table.
3. Built `lib/audit/` with `writeAuditLog` (soft-fail when DB is missing), `listRecentAuditLogs`, and action constants (`user.role_change`, `budget.update`, `ticket.escalate`).
4. Wired audits on Auth0 claim role sync when `users.role` actually changes, and added admin-only `PATCH /api/admin/users/[id]/role` with audit meta (`from` / `to` / source).
5. Extended Admin with a role-change form and a Recent audit card
