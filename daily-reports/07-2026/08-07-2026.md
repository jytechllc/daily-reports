### Mengchun Jiang
1. Daily Report:
2. Added Next.js `output: "standalone"` config so the AWS ECS/Fargate Docker image ships a self-contained server bundle instead of the full node_modules tree.
3. Authored a multi-stage Dockerfile (Debian slim base, non-root nextjs user) that builds and runs the standalone bundle on port 3000.
4. Added `.dockerignore` to trim the build context (node_modules, .next, .git, .github, env files, electron/release artifacts).
5. Made the stock-sim S3 client build-safe: `s3()` returns null and readers fall back when AWS creds are absent, so production builds no longer fail on the /stock-sim ISR prerender.

Plan:
1. Wire `.github/workflows/deploy.yml` (ECR build/push + ECS deploy) once Vincent provides the ECR repo URI and IAM deploy creds.

### Vincent Cheng
1. Daily Report:
2. Built the VPC networking piece of P3 in Terraform
3. Exposed subnet/VPC IDs as outputs for the ALB, ECS, and RDS modules to consume later.
4. Added`.gitignore`rules to keep Terraform state and real credentials out of the repo.
5. Validated the module (`terraform fmt`,`validate`,`init`)
