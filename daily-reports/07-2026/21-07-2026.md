### Marin Varivoda
1. Daily report - Auth0 roles / route + API guards (Phase 0)
2. Created branch `feat/auth0-roles` to add role-based access on top of existing Auth0 login.
3. Built `lib/auth/` helpers: role types, Auth0 claim → internal role mapping, `users` upsert by `auth0_sub`, and `getAppUser` / `requirePageRole` / `requireApiRole`.
4. Added admin-only `/admin` page and `/forbidden` for unauthorized access; Admin nav link is role-gated.
5. Protected `GET /api/monitor` (`admin` / `exec` / `eng`) and `GET /api/health/db` (`admin`) with 401/403 when Auth0 is configured.
