### Mengchun Jiang
1. Daily Report:
2. Merged PR #88 adding the GitHub Actions workflow that deploys the containerized web app to AWS ECS/Fargate, completing the CI side of the P1 web-to-AWS migration.
3. Workflow builds the image, pushes it to ECR, then swaps the image on the existing ECS task definition and rolls the Fargate service on push to main.
4. Runner authenticates via GitHub OIDC (role-to-assume) with no long-lived AWS keys stored in the repo; runtime env/secrets and IAM roles stay on the ECS task definition.
5. Documented the required repo config to run green: vars AWS_REGION/ECR_REPOSITORY/ECS_CLUSTER/ECS_SERVICE/ECS_TASK_FAMILY/ECS_CONTAINER_NAME and secret AWS_DEPLOY_ROLE_ARN.

Plan:
1. Wire up the ECR/ECS resources, OIDC role, and region once Vincent's P3 infrastructure is ready, then verify the first live deploy.
