### Marin Varivoda
1. Daily report - Background jobs path (Phase 0)
2. Created branch `feat/background-jobs` to add a secure cron path for monitor sync and alerts.
3. Built `lib/jobs/` with cron secret auth (`Bearer` / `x-cron-secret`), `runMonitorJob` reusing `lib/checks/*`, and an `emitAlerts` stub (console today, Slack later).
4. Added `GET|POST /api/cron/monitor` (fail-closed without `CRON_SECRET`) and bypassed Auth0 for `/api/cron/*` in `proxy.ts` so schedulers work without a browser session.
5. Wired `vercel.json` (every 30 minutes), documented `CRON_SECRET` / `CRON_ALERT_UPTIME` in `.env.example` + README, and noted the endpoint on Admin.
6. Left `/api/monitor` role-gated for humans; verified with `npm run typecheck`.
