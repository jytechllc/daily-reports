### Mengchun Jiang
1. Daily Report:
2. Added close-to-tray on Windows/macOS so the AutoClaw desktop shell stays resident and native notifications keep working, with quit-on-close fallback on Linux where the tray is unavailable.
3. Persisted window size, position, and maximized state across launches, including portable/USB builds that keep state on the drive folder.
4. Added renderer-crash recovery that re-navigates to the hosted app, and an unresponsive-page dialog that force-crashes a wedged renderer so reload works even on a hard-stuck main thread.

Plan:
1. Add a hardware-acceleration fallback switch for GPU-flaky Windows machines. Add auto update function.
