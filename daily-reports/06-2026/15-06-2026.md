### Mengchun Jiang
1. Daily Report:
2. Fixed the 2-month production deploy freeze.
3. Shipped a Windows portable "USB" build, built it as ia32 so a single exe runs on any 32/64-bit Windows, with the nsis installer staying x64.
4. Localized the GitHub Release notes into the 5 shipped locales (zh/en/ja/ko/es).

Plan:
1. After a read-only `migrate status` check, apply the ~5 pending (additive, no data-loss) DB migrations to prod via `prisma migrate deploy` so YouTube/BYO features work at runtime.
